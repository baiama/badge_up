import 'package:flutter/material.dart';

class ParkingAuto extends StatelessWidget {
  const ParkingAuto({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Парковать авто'),
      ),
    );
  }
}
